{
  "name": "worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tsc -b -v --watch",
    "build": "tsc -b -v",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "lint": "eslint --fix",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@workspace/node-shared": "workspace:*",
    "bullmq": "^5.36.0",
    "ioredis": "^5.4.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.7"
  }
}
